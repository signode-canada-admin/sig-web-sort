<% const get_status = (lst_status, order) => {
    ret  = lst_status.filter(obj => obj["_id"] === order)
    return ret
} %>


<div class="stageSearchHighlight">
    <% const orderStat = get_status(lst_status, order["_id"]); %>
    <% if (orderStat[0]) { %>
        <% if (orderStat[0]["staged"]) { %>
            <span>
                <%= "STAGED" %>
            </span>
        <% } else { %>
                <%= "" %>
        <% } %>
    <% } else { %>
        <%= "" %>
    <% } %>
</div>
